# test_webrtc

Install node and peerjs:
```bash
curl -sL https://deb.nodesource.com/setup_5.x | bash -
apt-get install -y nodejs
npm install peer
npm install express
```

Run peerjs server:
```bash
node peer_server.js
```


http://localhost:9999
